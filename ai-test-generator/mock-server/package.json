{
  "name": "ai-test-generator-mock-server",
  "version": "1.0.0",
  "description": "Mock API server for AI Test Case Generator frontend testing",
  "scripts": {
    "start": "json-server --watch db.json --port 5000 --routes routes.json --middlewares middleware.js",
    "dev": "json-server --watch db.json --port 5000 --routes routes.json --middlewares middleware.js --delay 1000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {}
}
